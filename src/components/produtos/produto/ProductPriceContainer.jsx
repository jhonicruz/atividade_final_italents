import React from 'react';

export const ProductPriceContainer = ({ children }) => {
  return (
    <div className="flex flex-col items-center justify-center text-center w-full">{children}</div>
  );
};
